apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: project
  name: local-pv-claim
spec:
  #storageClassName: manual
  accessModes:
    #- ReadWriteOnce
    - ReadWriteMany  # Multi-pod support!
  resources:
    requests:
      storage: 1Gi