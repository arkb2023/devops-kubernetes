apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: greeter-route
  namespace: exercises
spec:
  parentRefs:
  - group: ""
    kind: Service
    name: greeter-svc
    port: 8000
  rules:
  - backendRefs:
    - name: greeter-svc-v1
      port: 8000
      weight: 75
    - name: greeter-svc-v2
      port: 8000
      weight: 25
